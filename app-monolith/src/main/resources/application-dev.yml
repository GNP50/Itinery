spring:
  jpa:
    show-sql: true
  flyway:
    enabled: true
logging:
  level:
    com.travel: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql: TRACE

grpc:
  server:
    port: 9091
